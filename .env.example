# Kateb (كاتِب) - Environment Variables
# This Tauri desktop app stores settings in SQLite and API keys in OS keychain.
# These variables are only needed for development/testing.

# Development server port (optional, defaults to 3654)
# NUXT_PORT=3654

# Note: API keys for AI providers (Claude, OpenAI, Gemini, Grok)
# are configured through the Settings UI and stored securely in the OS keychain.
# Do NOT put API keys in this file.
